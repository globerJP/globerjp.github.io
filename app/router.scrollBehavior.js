// export default function (to, from, savedPosition) {
//     if (savedPosition) {
//       return {...savedPosition, behavior: 'smooth'}
//     }
// }
export default function (to, from, savedPosition) {
  // if (savedPosition) {
  //   return new Promise((resolve, reject) => {
  //     setTimeout(() => {
  //       resolve({ ...savedPosition })
  //     }, 5)
  //   })
  // } else if(to && to.name === 'feed') {
  //   return new Promise((resolve, reject) => {
  //     setTimeout(() => {
  //       resolve({x:0, y:0 })
  //     }, 5)
  //   })
  // } else {
  //   return false
  // }
  
}